/**
 * Data-driven game content package.
 *
 * Pure TypeScript. This package MUST NOT import Phaser or any DOM APIs.
 * Fighter/move/stage definitions, copy, distribution hooks, and asset manifest
 * live here so designers can tune the game without touching systems code.
 */

/** Content package version. */
export const CONTENT_VERSION = '0.0.0';
