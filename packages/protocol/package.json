{
  "name": "@rpr/protocol",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared protocol types and trace encoding — consumed by web and api. No Phaser, no DOM.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  }
}
